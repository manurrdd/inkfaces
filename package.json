{
  "name": "inkfaces-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Hand-drawn procedural avatars. Monorepo root.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "npm run build -w inkfaces",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev": "npm run dev -w @inkfaces/web",
    "dev:api": "npm run dev -w @inkfaces/api"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
